"""
Offline evaluation harness for the recommendation model (algorithm 2.0, Phase 0).

Scores any model version against a held-out ground-truth set so every later change
to the embedding/representation can be measured instead of guessed. See README.md.
"""